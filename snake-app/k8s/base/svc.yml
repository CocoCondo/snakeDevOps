apiVersion: v1
kind: Service
metadata:
  name: snake-svc
spec:
  selector:
    app: snake
    color: blue   # ← Jenkins cambia esto a green en blue/green
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP